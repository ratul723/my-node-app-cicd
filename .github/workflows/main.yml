name: new pipeline

on: [push]

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install node js
        uses: actions/setup-node@v4
        with:
          node-version: "lts/*" # Using LTS version

      - name: Install packages
        run: npm install

      - name: Test the application
        run: npm run test # Corrected test command
